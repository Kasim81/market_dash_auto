"""calculators — Phase E indicator calculators, split by region from
compute_macro_market.py (C7). The registry (_ALL_CALCULATORS) and the
output builders remain in compute_macro_market.py, which star-imports the
_calc_* functions from these modules."""
