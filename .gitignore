# Python bytecode
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
.venv/
venv/
env/

# Data outputs — track CSVs but ignore large/binary files if any are added
data/*.db
data/*.sqlite

# SEC EDGAR local cache (company_tickers.json, 7-day TTL) — re-fetched on demand
data/.sec_edgar_cache/

# Environment / secrets
.env

# OS
.DS_Store

# HTML builder intermediate payloads (embedded into final HTML in build step)
docs/payload_main.json
docs/payload_mkt.json

# Subagent worktree mounts (temporary; per-agent isolated working copies)
.claude/worktrees/
